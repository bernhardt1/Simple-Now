function createNavigationStateForExercise() {
  const linkingState = {
    index: 0,
    routes: [
      {
        name: 'Exercise',
      },
    ],
  };

  return linkingState;
}

export default createNavigationStateForExercise;
