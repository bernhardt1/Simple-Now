import React from 'react';

import { HomeScreen } from '../screenComponents/HomeScreen/index';

const Home = ({ navigation }) => {
  return <HomeScreen navigation={navigation} />;
};

export default Home;
