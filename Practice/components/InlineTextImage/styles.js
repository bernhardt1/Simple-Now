const {widthUnit} = require('../../styles/constants');

const styles = {
  image: {
    height: widthUnit * 4,
    width: widthUnit * 4,
    marginHorizontal: widthUnit,
  },
};

export default styles;
