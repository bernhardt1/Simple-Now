import { DARK_OVERLAY, VERY_DARK_OVERLAY } from '../../styles/colors';
import { screenWidth, statusBarHeight } from '../../styles/constants';

const styles = {
  container: {
    height: statusBarHeight,
    width: screenWidth,
    backgroundColor: VERY_DARK_OVERLAY,
  },
};

export default styles;
