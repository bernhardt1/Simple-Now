import { buttonFont } from '../../styles/fonts';
import { standardButton } from '../../styles/standardComponents';

const styles = {
  image: {
    height: standardButton.height * 0.8,
    width: standardButton.height * 0.8,
  },
};

export default styles;
