import React from 'react';

import { TimerScreen } from '../screenComponents/TimerScreen/index';

const Timer = ({ navigation }) => {
  return <TimerScreen navigation={navigation} />;
};

export default Timer;
