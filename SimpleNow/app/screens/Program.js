import React from 'react';

import ProgramScreen from '../screenComponents/ProgramScreen/ProgramScreen';

const Program = ({ navigation, route }) => {
  return <ProgramScreen navigation={navigation} route={route} />;
};

export default Program;
