import React from 'react';
import { SetBellIntervalScreen } from '../screenComponents/SetBellIntervalScreen';

const SetBellInterval = ({ navigation }) => {
  return <SetBellIntervalScreen navigation={navigation} />;
};

export default SetBellInterval;
