import {BRAND_WHITE} from '../../styles/colors';
import {standardFlair} from '../../styles/standardComponents';

const styles = {
  container: {
    ...standardFlair,
    backgroundColor: BRAND_WHITE,
  },
};

export default styles;
