import React from 'react';
import ExploreScreen from '../screenComponents/ExploreScreen/ExploreScreen';

const Explore = ({ background, navigation }) => {
  return <ExploreScreen navigation={navigation} />;
};

export default Explore;
